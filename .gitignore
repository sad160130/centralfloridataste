.env
.dietary_llm_ckpt.json
__pycache__/
node_modules
.astro/

# local paid-API image pilots (not for commit)
pilot-places-photos/
pilot-streetview/

# restaurant photos are served from Vercel Blob; local copies are a byte cache only
public/restaurant-photos/

# Built output — Vercel runs `astro build` and generates this on deploy,
# so committing it only duplicated the source of truth.
dist/
